import { accountSchema, payloadSchema, returnAccountSchema } from "./account.schemas"
import { loginSchema } from "./login.schemas"
import { courseCreationSchema, coursePayloadSchema, courseUpdateSchema } from "./course.schemas"

export {
    accountSchema,
    payloadSchema,
    returnAccountSchema,
    loginSchema,
    courseCreationSchema,
    coursePayloadSchema,
    courseUpdateSchema
}